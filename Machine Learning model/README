# Machine Learning model

Since the ways in how the lack of attention is expressed is different in each person the algorithm for the classification of the levels of attention needs to be trained. Here we showed an example with randomly generated data just to show how the script of the model would be. 
